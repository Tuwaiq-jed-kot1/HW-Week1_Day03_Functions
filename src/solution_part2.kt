// Q3
fun main(){
    var fValue = 7
    var sValue = 70
    var tValue = 20
    if (fValue > sValue && fValue > tValue)
        println(" the largest value is $fValue")
    else if (sValue > fValue && sValue > tValue)
        println(" the largest value is $sValue")
    else if (tValue > sValue && tValue > sValue)
        println(" the largest value is $tValue") // طولتها وهي قصيرة بس لسا بتعلم :(

}